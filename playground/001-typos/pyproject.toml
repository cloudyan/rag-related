[project]
name = "gen-typos"
version = "0.1.0"
description = "错别字纠错练习短文批量生成器"
authors = [
    {name = "Your Name", email = "your.email@example.com"}
]
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "reportlab>=4.0.0",
]
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
